# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4
PortSystem          1.0
PortGroup           github 1.0
PortGroup           python 1.0

github.setup        jazzband pip-tools 7.4.1
distname            ${github.version}
worksrcdir          ${github.project}-${github.version}
fetch.type          git
git.url             https://github.com/${github.author}/${github.project}.git
git.branch          ${github.version}

name                py-pip-tools
license             MIT
platforms           darwin
maintainers         {@jojojol gmail.com:rjnakatani} openmaintainer
description         A set of command line tools to help you keep your pip-based packages fresh, even when you've pinned them. 
long_description    A set of command line tools to help you keep your pip-based packages fresh, even when you've pinned them.You do pin them, right? (In building your Python application and its dependencies for production, you want to make sure that your builds are predictable and deterministic.)
homepage            https://github.com/jazzband/pip-tools
checksums           rmd160  b6113432f11aa3de785122b528d54b55d9ce570e \
                    sha256  15c3c53e584ddf6ecdb93cbb44194943f283d13606417629182ae72ad7eb2674 \
                    size    132843

python.versions     36 37 38 39 310

if {${name} ne ${subport}} {

    depends_build-append   port:py${python.version}-setuptools_scm \
                           port:py${python.version}-wheel
    
    livecheck.type         none

} else {

    livecheck.regex        archive/[join ${github.tag_prefix} ""](1\[^"\]+)${extract.suffix}


}
